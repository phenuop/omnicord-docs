---
title: ".combine"
description: "Forge a new attack by combining two base attacks using an interactive select menu system."
---

import { CommandTips } from '@/components/command-tips';

## Overview

Forge a new attack by combining two base attacks using an interactive select menu system.

Allows you to combine two base attacks to create a new, more powerful attack using an interactive select menu system.

## Usage

```
.combine
```

**Aliases:** `.forge`, `.synthesize`, `.synthesis`, `.ironworks`, `.smith`, `.smitting`

## Features

#### Attack combination system

#### Rare combinations create unique attacks

#### Select menu interface

#### Upgrade path tracking

#### Cost display before confirmation

## Requirements

- **nullite**: Base damage × 0.8 + variance
- **fusionCore**: Required for 60+ DMG techniques

## Examples

```
.combine - Open the combine menu to select attacks to combine
```

## Tips

<CommandTips items={["Some combinations create rare attacks","Not all attacks can be combined","Combined attacks are often stronger","Requirements: Nullite = Base damage × 0.8 + variance, Fusion Core required for 60+ DMG techniques"]} />
